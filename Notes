How to tell git who you are?
git config --gloal user.name "<username>"
git config --global user.email "<email-id>"


Uploading My Code To Github (Empty Repo):
git init
git add (file name_)
git commit -m "Added file name (kuch bhe likh sakte ho)
git remote add in <repo Link>
git checkout -b <branch name> (b use for create a new branch.open existing branch)
git status
git push in <branchname>
git fetch (TO UPDATE CODE)

Doing Changes To My Code

Cloning Repo
git clone <repo-url> -b <branch-name> <folder-name>
git add -A (A is used fopr all)
git rebase -i HEAD~jitani comits dehkhane hai uatna number (ctrl+o,enter,ctrl+x)